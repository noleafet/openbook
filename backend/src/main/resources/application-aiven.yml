# Production logging adjustments
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: ERROR
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"


management:
  endpoints:
    web:
      exposure:
        # Expose the metrics endpoint safely
        include: "health,prometheus"
  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    tags:
      # Automatically adds an "application" tag to all your metrics
      application: "backend-service" 

spring:
  application:
    name: openbook
  datasource:
    # Changed sslmode=disable to sslmode=require for Aiven compliance
    url: ${SPRING_DATASOURCE_URL}?sslmode=require
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver 

    # HikariCP pooling limits tailored for Aiven Free Tier
    hikari:
      maximum-pool-size: 4
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 20000

  jackson:
    default-property-inclusion: non_null
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        # To avoid configuration on code-level, explicit redeclaration of connection parameters is done here in the configuration file
        connection.provider_class: org.hibernate.hikaricp.internal.HikariCPConnectionProvider

        connection.url: ${SPRING_DATASOURCE_URL}?sslmode=require
        connection.username: ${SPRING_DATASOURCE_USERNAME}
        connection.password: ${SPRING_DATASOURCE_PASSWORD}

        hikari.maximumPoolSize: 4
        hikari.minimumIdle: 2
        hikari.idleTimeout: 30000
        hikari.maxLifetime: 600000
        connection-timeout: 20000
        
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: validate
        naming:
          physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy


server:
  port: ${SPRING_SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /api-docs.html
    syntaxHighlight:
      theme: light

jwt:
  secret: ${JWT_SECRET:8284D6251655468576D5A713474371805096859703373367639792F423F45284}
  expiration: 86400000 # 24 hours in milliseconds

# disable flyway execution
#spring.flyway.enabled=false

# If you have an existing database and start using Flyway for new database changes.
#spring.flyway.baseline-on-migrate=true

# To customize flyway migrations tracking table name
#spring.flyway.table=db_migrations

# In case of any flyway execution errors, clean up the database and re-run all the migrations
## NEVER USE THIS IN PRODUCTION. ONLY SUITABLE FOR PROTOTYPING
#spring.flyway.clean-disabled=false
#spring.flyway.clean-on-validation-error=true
